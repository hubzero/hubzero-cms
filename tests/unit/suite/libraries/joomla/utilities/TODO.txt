
JError
1	check if handleEcho() and getBacktrace() work in common;
	they appear to "conflict"

JArrayHelper
1	what are the differences/advantages of 
	JArrayHelper::fromObject() vs. JObject::getPublicProperties()
	(findings might be more of an interst to the doc team)

