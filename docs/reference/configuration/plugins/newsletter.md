<!--
status: generated
source: core/plugins/newsletter/*/*.xml
-->

# Newsletter plugins

Parameters of every plugin in the `newsletter` group, from each plugin's manifest. Set them under **Extensions > Plugins** in the administrator interface.

## Newsletter - Event (`plg_newsletter_event`)

Newsletter plugin for Events

This plugin has no parameters.

## Newsletter - Jobs (`plg_newsletter_jobs`)

Newsletter plugin for Jobs

This plugin has no parameters.

## Newsletter - Resource (`plg_newsletter_resource`)

Newsletter plugin for Resource

This plugin has no parameters.
