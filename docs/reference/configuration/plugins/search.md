<!--
status: generated
source: core/plugins/search/*/*.xml
-->

# Search plugins

Parameters of every plugin in the `search` group, from each plugin's manifest. Set them under **Extensions > Plugins** in the administrator interface.

## Search - Blogs (`plg_search_blogs`)

Search plugin for blogs

This plugin has no parameters.

## Search - Citations (`plg_search_citations`)

Search plugin for citations

This plugin has no parameters.

## Search - Collections (`plg_search_collections`)

Search plugin for collections

This plugin has no parameters.

## Search - Content (`plg_search_content`)

Search plugin for content

This plugin has no parameters.

## Search - Courses (`plg_search_courses`)

Search plugin for courses

This plugin has no parameters.

## Search - Events (`plg_search_events`)

Search plugin for events

This plugin has no parameters.

## Search - Forum (`plg_search_forum`)

Search plugin for forum

This plugin has no parameters.

## Search - Groups (`plg_search_groups`)

Search plugin for groups

This plugin has no parameters.

## Search - Kb (`plg_search_kb`)

Search plugin for kb

This plugin has no parameters.

## Search - Members (`plg_search_members`)

Search plugin for members

This plugin has no parameters.

## Search - Projects (`plg_search_projects`)

Search plugin for projects

This plugin has no parameters.

## Search - Publications (`plg_search_publications`)

Search plugin for publications

This plugin has no parameters.

## Search - Questions (`plg_search_questions`)

Search plugin for questions

This plugin has no parameters.

## Search - Solr (`plg_search_remote`)

Search plugin for remote

### Connection Info

| Parameter | Label | Type | Default | Description |
|---|---|---|---|---|
| `app_url` | Remote URL | text | — | Target URL of remote solr mirror. |
| `app_token` | Token | text | — | API Token to connect to remote URL |

## Search - Resources (`plg_search_resources`)

Search plugin for resources

This plugin has no parameters.

## Search - Sitemap (`plg_search_sitemap`)

Search plugin for sitemap

This plugin has no parameters.

## Search - Solr (`plg_search_solr`)

Search plugin for blogs

This plugin has no parameters.

## Search - Sortcourses (`plg_search_sortcourses`)

Search plugin for sourtcourses

This plugin has no parameters.

## Search - Sortevents (`plg_search_sortevents`)

Search plugin for sortevents

This plugin has no parameters.

## Search - Suffixes (`plg_search_suffixes`)

Search plugin for suffixes

This plugin has no parameters.

## Search - Resources (`plg_search_tickets`)

Search plugin for resources

This plugin has no parameters.

## Search - Weightcontributor (`plg_search_weightcontributor`)

Search plugin for weightcontributor

This plugin has no parameters.

## Search - Weighttitle (`plg_search_weighttitle`)

Search plugin for weighttitle

This plugin has no parameters.

## Search - Weighttools (`plg_search_weighttools`)

Search plugin for weighttools

This plugin has no parameters.

## Search - Wiki (`plg_search_wiki`)

Search plugin for wiki

This plugin has no parameters.

## Search - Wishlists (`plg_search_wishlists`)

Search plugin for wishlists

This plugin has no parameters.
