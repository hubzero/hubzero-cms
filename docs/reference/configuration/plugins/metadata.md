<!--
status: generated
source: core/plugins/metadata/*/*.xml
-->

# Metadata plugins

Parameters of every plugin in the `metadata` group, from each plugin's manifest. Set them under **Extensions > Plugins** in the administrator interface.

## Metadata - Local (`plg_metadata_local`)

Local metadata plugin handler

This plugin has no parameters.
