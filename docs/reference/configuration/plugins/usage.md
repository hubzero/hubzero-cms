<!--
status: generated
source: core/plugins/usage/*/*.xml
-->

# Usage plugins

Parameters of every plugin in the `usage` group, from each plugin's manifest. Set them under **Extensions > Plugins** in the administrator interface.

## Usage - Domain Class (`plg_usage_domainclass`)

Domain Class display for Usage component

This plugin has no parameters.

## Usage - Domains (`plg_usage_domains`)

Domains display for Usage component

This plugin has no parameters.

## Usage - Maps (`plg_usage_maps`)

Maps display for Usage component

This plugin has no parameters.

## Usage - Overview (`plg_usage_overview`)

Overview display for Usage component

### Basic

| Parameter | Label | Type | Default | Description |
|---|---|---|---|---|
| `currentYear` | End Year | text | — | The most current date to end usage for. Leaving blank = 'now' |
| `currentMonth` | End Month | text | — | The most current date to end usage for. Leaving blank = 'now' |
| `message` | Message | textarea | — | A message to display to the users. |

## Usage - Partners (`plg_usage_partners`)

Partners display for Usage component

This plugin has no parameters.

## Usage - Region (`plg_usage_region`)

Region display for Usage component

This plugin has no parameters.

## Usage - Tools (`plg_usage_tools`)

Tools display for Usage component

This plugin has no parameters.
