<!--
status: generated
source: core/components/com_tags/config/config.xml
-->

# Tags (com_tags)

Parameters from [`core/components/com_tags/config/config.xml`](../../../../core/components/com_tags/config/config.xml), as shown on the component's **Options** screen in the administrator interface.

## Basic

| Parameter | Label | Type | Default | Description |
|---|---|---|---|---|
| `focus_area_01` | Focus Area 1 | text | — | A tag that is to serve as a focus area for the site. |
| `focus_area_02` | Focus Area 2 | text | — | A tag that is to serve as a focus area for the site. |
| `focus_area_03` | Focus Area 3 | text | — | A tag that is to serve as a focus area for the site. |
| `focus_area_04` | Focus Area 4 | text | — | A tag that is to serve as a focus area for the site. |
| `focus_area_05` | Focus Area 5 | text | — | A tag that is to serve as a focus area for the site. |
| `focus_area_06` | Focus Area 6 | text | — | A tag that is to serve as a focus area for the site. |
| `focus_area_07` | Focus Area 7 | text | — | A tag that is to serve as a focus area for the site. |
| `focus_area_08` | Focus Area 8 | text | — | A tag that is to serve as a focus area for the site. |
| `focus_area_09` | Focus Area 9 | text | — | A tag that is to serve as a focus area for the site. |
| `focus_area_10` | Focus Area 10 | text | — | A tag that is to serve as a focus area for the site. |

## Cache

| Parameter | Label | Type | Default | Description |
|---|---|---|---|---|
| `cache` | Cache Enabled | list | `1 (Yes)` | To improve page performance, tag clouds can be cached. Options: `0` No, `1` Yes. |
| `cache_time` | Cache Time | text | `15` | The time, in minutes, that data should be cached. |
